@using SiteExpensesManagement.App.Contracts.ViewModels
@model RoomTypeViewModel

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.CountOfRooms)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.CountOfRooms)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.CreatedAt)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.CreatedAt)
        </dd>
    </dl>
    
    <form asp-action="Delete" asp-controller="RoomTypes" method="post">
        <input type="hidden" asp-for="@Model.Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index" asp-controller="RoomTypes" class="btn btn-success">Back to List</a>
    </form>
</div>
